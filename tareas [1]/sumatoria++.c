#include<stdio.h>
#include<math.h>

int main(){

    int n=0, l=1, m=0;
    double ac=0;

    printf("ingrese el valor de n");
        scanf("%d", &n);
    

    for ((i=2), (i<=n),i++){


        for((k=1), (k<=i), k++){

           l = (f*k);
           

        }

      ac= (ac + ((l*3)*(i+1))/(pow((i-1),3)));
      
    }

    ac= ac*5    


    return 0;
}